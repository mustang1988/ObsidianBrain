---
Arguments:
  - Name: ""
    Type: ""
    Required: 
    Comment: ""
    Remark: ""
    Default: ""
---
```dataviewjs
const data = dv.current().Arguments.map(arg => [arg.Name, arg.Type, arg.Required, arg.Comment, arg.Remark, arg.Default]);
dv.table(
	["参数名", "参数类型", "必填?", "参数说明", "备注", "默认值"],
	data
);
```
