---
aliases: ["库","框架"]
---

## 框架&库

### 框架
```dataviewjs
dv.table(
	["名称", "简介", "语言",],
	dv.pages('#框架')
	.filter(lib => lib.file.path != dv.current().file.path)
	.sort(lib => lib.file.name)
	.map(lib => [lib.file.link,  lib.Abstract, lib.Language,])
);
```

### 库
```dataviewjs
dv.table(
	["名称", "简介","语言"],
	dv.pages('#库')
	.filter(lib => lib.file.path != dv.current().file.path)
	.sort(lib => [lib.Language, lib.file.name], 'asc')
	.map(lib => [lib.file.link, `${lib.Abstract.substring(0,80)}...`, lib.Language])
);
```

#框架 #库 